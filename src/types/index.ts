export type SubjectType = {
  subject: string;
  professors: string[];
  id: string;
};

export type SelectType = {
  label: string;
  id: string;
};

export type ProfessorType = {
  name: string;
  faculties: string[];
  id: string;
};
